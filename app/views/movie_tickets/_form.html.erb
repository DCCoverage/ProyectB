<%= form_for @movie_ticket, url:url, method:method do |form| %>
  <% if @movie_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie_ticket.errors.count, "error") %> prohibited this movie_ticket from being saved:</h2>

      <ul>
        <% @movie_ticket.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :row %>
    <%= form.button_field :row %>
  </div>

  <div class="field">
    <%= form.label :column %>
    <%= form.number_field :column %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
