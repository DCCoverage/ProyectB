<p id="notice"><%= notice %></p>

<h1>Show Times</h1>

<table>
  <thead>
    <tr>
      <th>Movie</th>
      <th>Room</th>
      <th>When</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @show_times.each do |show_time| %>
      <tr>
        <td><%= show_time.movie_id %></td>
        <td><%= show_time.room_id %></td>
        <td><%= show_time.date %></td>
        <td><%= show_time.when %></td>
        <td><%= link_to 'Show', show_time %></td>
        <td><%= link_to 'Edit', edit_show_time_path(show_time) %></td>
        <td><%= link_to 'Destroy', show_time, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
